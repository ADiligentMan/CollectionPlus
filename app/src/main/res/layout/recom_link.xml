<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="recomLink"
            type="com.example.acer.collectionplus.JavaBean.SimpleRecomLinkBean" />
        <variable
            name="recomLinkAdapter"
            type="com.example.acer.collectionplus.Adapter.RecomLinkAdapter" />
    </data>
    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/recom_user_border"
        android:padding="4dp"
        android:layout_marginTop="5dp"
        android:onClick="@{()->recomLinkAdapter.clickLink(recomLink.value)}">
        <ImageView
            android:layout_width="120dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_foreground"
            android:id="@+id/recom_link_image"
            android:layout_marginTop="5dp"
            app:layout_constraintHorizontal_weight="1"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:imageUrl="@{recomLink.picPath}" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintHorizontal_weight="2"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{recomLink.title}"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="@{recomLink.date}"/>
    </android.support.constraint.ConstraintLayout>
</layout>
